This is an additional workspace (the third)
it is used to have a Gazebo Plugin as a ROS package, this ROS package is loaded as default and thus the plugin should be available by default, not loaded by default, an additional variable or launch file run is still necesarry!


